{
  "name": "vite-plugin-estrela",
  "version": "1.4.3",
  "description": "A vite plugin to process estrela jsx/tsx files",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn clean && yarn compile && tsc",
    "clean": "rimraf dist",
    "compile": "esbuild $(esbuild-config)",
    "prepublish": "yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/core": "^7.0.0",
    "babel-plugin-estrela": "^0.2.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.24",
    "esbuild": "^0.14.38",
    "esbuild-config": "^0.2.0",
    "rimraf": "^3.0.2"
  },
  "peerDependencies": {
    "vite": "^2.9.0"
  }
}